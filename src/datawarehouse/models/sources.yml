version: 2

sources:
  - name: raw
    description: "Raw NYC 311 data stored in DuckDB"
    database: data
    schema: main
    tables:
      - name: nyc311_csv
        description: "Raw NYC 311 data table in DuckDB" 